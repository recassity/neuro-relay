nakurity-backend: # the emulated version of the twin's backend, with multiplexing capabilities.
  host: "127.0.0.1"
  port: 8001

nakurity-client: # the client that forwards information to neuro's/evil's backend
  host: "127.0.0.1"
  port: 8000
